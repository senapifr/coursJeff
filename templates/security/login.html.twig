{% extends 'base.html.twig' %}

{% block body %}

    <form action="{{ path('security_login') }}" method="post" >
        <div class="form-group">
            <input class="form-control" type="text" name="_username" placehollder="Email ...." required />
            <input class="form-control" type="password" name="_password" placehollder="Mot de passe ...." required />
        </div>
        <div class="form-group">
            <button type="submit" class="btn btn-success">Connexion !</button>
        </div>
    </form>

{% endblock %}